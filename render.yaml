# នៅក្នុង render.yaml
services:
  - type: web
    name: DocConvert-bot
    env: python
    
    # === ត្រូវប្រាកដថា buildCommand មានលក្ខណៈដូចនេះ ===
    buildCommand: apt-get update && apt-get install -y ffmpeg tesseract-ocr -y && pip install -r requirements.txt
    
    # ... (StartCommand របស់អ្នកគឺ python main.py) ...
